@using DoAnTotNghiep.Data
@model PagedList.IPagedList<DoAnTotNghiep.Models.DTO.ContactDTO>
@{
    var filter = ViewData["Filter"] as DoAnTotNghiep.Data.Filter;
}

<div style="padding: 40px">
    <div class="row">
        <div class="col-md-6">
            <h4 class="fw-bold pb-3 mb-4"><span class="text-muted fw-light">Contact /</span> Contact List</h4>
        </div>
        <div class="col-md-6 ">
            @*<div class="flex-row justify-content-end" style="display: flex">
                    <a class="btn btn-primary" style="margin-right: 10px" href="@Url.Action("Create")">Thêm mới</a>
                </div>*@
        </div>
    </div>
    <div class="card">
        <form id="form" class="m-3">
            @Html.HiddenFor(m => filter.PageIndex)
            @Html.HiddenFor(m => filter.PageSize)
            <div class="row">
                <div class="col-md-4">
                    <label>Từ khóa</label>
                    @Html.TextBoxFor(m => filter.Keyword, new { @class = "form-control", @placeholder = "Enter keyword: contact name, email", @type = "text" })
                </div>
                <div class="col-md-2" style="display: flex; flex-direction: column;justify-content: end;">
                    <span class="input-group mb-0">
                        <button type="submit" class="btn btn-info mb-0"><i class="fa fa-search"></i> Search</button>
                    </span>
                </div>
            </div>
        </form>
        <div class="table-responsive text-nowrap">
            <table class="table table-striped table-bordered mb-3">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Contact name</th>
                        <th>Email</th>
                        <th>Status</th>
                        <th>Public</th>
                        <th>Responder</th>
                        <th>Function</th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int stt = (Model.PageNumber - 1) * Model.PageSize + 1;
                    }
                    @if (Model.Count() > 0)
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@stt</td>
                                <td>@item.Fullname</td>
                                <td>@item.Email</td>
                                <td>@(item.Flag == (int)ContactFlagStatus.Tra_Loi ? "Answered" : "Unanswered")</td>
                                <td>
                                    @(item.Status == (int)ContactStatus.Cong_Khai ? "Public" : "Hidden")
                                </td>
                                <td>@item.UpdatedUserName</td>
                                <td>
                                    @if (item.Status == (int)ContactStatus.Cong_Khai && item.Flag == (int)ContactFlagStatus.Tra_Loi)
                                    {
                                        <a class="btn btn-secondary" href="@Url.Action("UpdateStatus", new { id = item.ID_Contact, status = (int)ContactStatus.An })">Hidden</a>
                                    }
                                    else if (item.Status == (int)ContactStatus.An && item.Flag == (int)ContactFlagStatus.Tra_Loi)
                                    {
                                        <a class="btn btn-success" href="@Url.Action("UpdateStatus", new { id = item.ID_Contact, status = (int)ContactStatus.Cong_Khai })">Public</a>
                                    }
                                    <a class="btn btn-warning" href="@Url.Action("Edit", new { id = item.ID_Contact })">Reply</a>
                                    <a class="btn btn-danger" href="@Url.Action("Delete", new { id = item.ID_Contact })">Delete</a>
                                </td>
                            </tr>
                            stt++;
                        }
                    }

                </tbody>
            </table>

            <div class="d-flex flex-row justify-content-end p-2">
                @if (Model.Count() > 0)
                {
                    <nav aria-label="...">
                        <ul class="pagination">
                            <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                                <a class="page-link" onclick="toPage(1)" style="cursor: pointer">Head</a>
                            </li>
                            <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                                <a class="page-link" onclick="toPage(@(Model.PageNumber - 1))" style="cursor: pointer">Backward</a>
                            </li>
                            @if (Model.Count() > 0)
                            {
                                for (int i = 1; i <= Model.PageCount; i++)
                                {
                                    <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                                        <a class="page-link" onclick="toPage(@i)" style="cursor: pointer">@i</a>
                                    </li>
                                }
                            }
                            <li class="page-item @(Model.PageNumber == Model.PageCount ? "disabled" : "")">
                                <a class="page-link" onclick="toPage(@(Model.PageNumber + 1))" style="cursor: pointer">Next</a>
                            </li>
                            <li class="page-item @(Model.PageNumber == Model.PageCount ? "disabled" : "")">
                                <a class="page-link" onclick="toPage(@(Model.PageCount))" style="cursor: pointer">End</a>
                            </li>
                        </ul>
                    </nav>
                }
            </div>
        </div>
    </div>

    <hr class="my-5" />

</div>

<script>
    function toPage(index) {
        $("#filter_PageIndex").val(index)
        $("#form").submit()
    }
</script>
@using DoAnTotNghiep.Models
@model List<Category>
@foreach (var item in Model)
{
    <li><a href="@Url.Action("ProductByCategory", "Product", new { ID_Category = item.ID_Category})">@item.Category1</a></li>
}
